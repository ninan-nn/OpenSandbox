# OpenSandbox MCP Sandbox Server

OpenSandbox MCP Server exposes the OpenSandbox Python SDK as MCP tools for Claude Code, Cursor, and other MCP-capable clients.

> **Full documentation**: https://open-sandbox.ai/sdks/mcp

## Install

```bash
pip install opensandbox-mcp
```
