---
title: Execution API (execd)
---

# Execution API (execd)

execd runs inside the sandbox and provides commands, code, and filesystem APIs.

## Highlights

- Code execution with contexts
- Command execution with SSE
- File CRUD and search
- Metrics endpoints

Spec: `specs/execd-api.yaml`.
