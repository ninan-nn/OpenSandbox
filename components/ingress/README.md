# OpenSandbox Ingress

HTTP/WebSocket reverse proxy that routes to sandbox instances, supporting header-based and URI-based routing modes.

> **Full documentation**: https://open-sandbox.ai/components/ingress
