# execd - OpenSandbox Execution Daemon

The runtime daemon used inside OpenSandbox sandboxes, exposing HTTP APIs for code execution, shell commands, filesystem operations, PTY sessions, and metrics.

> **Full documentation**: https://open-sandbox.ai/components/execd
