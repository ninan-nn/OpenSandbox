# OpenSandbox CLI

`osb` is the command-line interface for OpenSandbox, providing sandbox lifecycle management, command execution, file operations, egress policy inspection, and agent skill installation.

> **Full documentation**: https://open-sandbox.ai/cli/

## Install

```bash
pip install opensandbox-cli
```
